module {{.Module}}

go 1.26

// moq (mock generator) recorded as a tool dependency so `make generate` runs it
// via `go tool moq` without a global install. `go mod tidy` resolves the version.
// (Protobuf codegen needs no tool deps: `make proto` uses buf's hosted plugins.)
tool github.com/matryer/moq
{{if .Replace}}
require github.com/assanoff/skit v0.0.0

replace github.com/assanoff/skit => {{.Replace}}
{{else if .SDKRequire}}
require github.com/assanoff/skit {{.SDKVersion}}
{{end}}